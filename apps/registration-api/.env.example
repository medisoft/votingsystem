NODE_ENV=development
HOST=0.0.0.0
PORT=3001
DATABASE_URL=postgresql://voting:voting@localhost:15432/registration?schema=public
ADMIN_ORIGIN=http://localhost:5173
LOG_LEVEL=info
# Development-only Ed25519 issuer private key (PKCS8 DER, base64url).
# Generate a new key for any non-local environment:
# node -e "const {generateKeyPairSync}=require('node:crypto'); console.log(generateKeyPairSync('ed25519').privateKey.export({type:'pkcs8',format:'der'}).toString('base64url'))"
ISSUER_PRIVATE_KEY=MC4CAQAwBQYDK2VwBCIEIDCy4kdw2B5wJxNs_n1-IOsJfn7KkG0u9UUM4Y_XFM31
ISSUER_KEY_VERSION=dev-2026-01
# ISSUER_PRIVATE_KEY_FILE=/run/secrets/issuer-ed25519.pem
# ISSUER_ID=condominium-registration-service
# Used only by `npm run db:seed`. Compose already seeds these locally.
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=ManualTest-2026
