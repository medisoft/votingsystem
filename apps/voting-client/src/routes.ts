/** Client routes for welcome, activation, home, and later screens. */
export const clientRoutes = {
  welcome: '/',
  activate: '/activate',
  home: '/home',
  proposals: '/proposals',
  votes: '/votes',
  settings: '/settings',
} as const;
